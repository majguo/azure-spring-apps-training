# Configure the MongoDB client, e.g., a single node cluster on localhost. Leave it empty if you run in dev mode
#quarkus.mongodb.connection-string = mongodb://localhost:27017
# Mandatory if you don't specify the name of the database using @MongoEntity
quarkus.mongodb.database = cities

# Configure these two properties as necessary to match the host and port of the Consul service
#consul.host=localhost
#consul.port=8500

# Configure these two properties as necessary to match the host and port of city service
#svc.host=localhost
#svc.port=8080
